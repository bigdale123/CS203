import java.math.*;
public class Library {
	public static double square_root(double value) {
		// System.out.println(value);
		return Math.sqrt(value); // square_root wasn't actually doing anything, changed code to take square root.
	}
}
